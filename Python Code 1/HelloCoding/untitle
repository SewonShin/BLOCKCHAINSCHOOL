# 딕셔너리
(일반적으로) 문자열을 기반으로 값을 저장하는 것
{
    <키>: <값>,
    <키>: <값>,
    ...
    <키>: <값>
}

예) 리스트: list_a[1] → 인덱스
예) 딕셔너리: dict_a["name"] → 키

# 딕셔너리 처리
- 추가: dict_a["new_name"] = 10 # 그냥 때려 넣으면 OK
- 제거: del dict_a["name"]
- 확인: key in dict_a